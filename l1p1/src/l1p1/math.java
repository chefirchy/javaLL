package l1p1;

public class math {
    public static void main(String args[]) {
      int a=10;
      int b=25;
      double x=Math.pow(a, 3);
      double y=Math.pow(b, 3);
      double z = ((x+y)/2);
      double y1 = x*y;
      double c = Math.sqrt(y1);
      System.out.println("serarif = " + z);
      System.out.println("sergeom = " + c);
    }
}